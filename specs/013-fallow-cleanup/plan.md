# Implementation Plan: Fallow Findings Cleanup

**Branch**: `013-fallow-cleanup` | **Date**: 2026-07-10 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `specs/013-fallow-cleanup/spec.md`

## Summary

Address all findings from fallow codebase analysis (dead code, duplication, complexity hotspots, security candidates) to establish a stable foundation before implementing further features. The cleanup covers dead-code removal (1 file, 3 types, 2 deps), pragmatic duplication extraction (6 patterns, target ~4% from 6.2%), complexity refactoring of 4 hotspot files, and suppression of 8 SSRF false positives in api.ts.

## Technical Context

**Language/Version**: TypeScript 6, React 19, Vite 8, ESM

**Primary Dependencies**: @tanstack/react-query v5, react-router-dom v7, react-hook-form v7, Zod v4, Tailwind v4, shadcn/ui

**Storage**: N/A (SPA with API backend)

**Testing**: Vitest v4 + @testing-library/react v16 + MSW v2 (233 existing tests)

**Target Platform**: Modern browsers (ES2023), responsive web

**Project Type**: Single-page application (SPA)

**Performance Goals**: Maintain existing — no new performance requirements

**Constraints**: All 233 existing tests must continue to pass (SC-006); tsc -b and ESLint must pass with zero errors (SC-007, SC-008); extractors must follow existing project conventions (named exports, cn(), shadcn primitives, @/ path alias)

**Scale/Scope**: 14 affected files across dead-code (4), duplication (14 files, 20 clone groups), complexity (7 hotspot files), security (1 file)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Analysis |
|-----------|--------|----------|
| I — TDD is Non-Negotiable | PASS (with note) | Refactoring-only feature — no new functionality. Existing test suite (233 tests) serves as regression guard (SC-006). Extracted shared components/hooks should have their own tests where they introduce new module boundaries. |
| II — Agent Skills Discipline | PASS | Load `tdd` and `git-commit` skills during implementation. |
| III — MCP-First Tooling | PASS | No new dependencies or UI components — shadcn MCP not required. |
| IV — TypeScript-First | PASS | Extractors and refactors must maintain strict typing — no `any`, preserve `@/` alias usage. |
| V — Component Design | PASS | Extracted shared components must follow existing patterns: named exports, `cn()` class merging, shadcn primitives from `@/components/ui/`, compound component pattern where appropriate. |
| VI — Best Practices | PASS | Must maintain API client patterns in api.ts restructure. No performance regressions from introducing new component layers. |

**Gates**: All pass. No violations to justify in Complexity Tracking.

## Project Structure

### Documentation (this feature)

```text
specs/013-fallow-cleanup/
├── plan.md               # This file
├── research.md           # Phase 0 output
├── data-model.md         # Phase 1 output (N/A — no new entities)
├── quickstart.md         # Phase 1 output (validation guide)
├── contracts/            # Phase 1 output (N/A — no new interfaces)
├── checklists/
│   └── requirements.md   # Quality checklist
└── tasks.md              # Generated by /speckit.tasks
```

### Source Code (repository root)

This feature does not introduce new source directories. It operates on existing files throughout the project:

```text
src/
├── lib/
│   └── api.ts                    # FR-015: Restructure (split request logic)
├── components/
│   ├── ui/
│   │   └── index.ts              # FR-001: Delete (unused file)
│   └── shared/                   # Target for extracted shared components
│       ├── PageHero.tsx          # FR-006: Hero section
│       ├── EventMetadata.tsx     # FR-007: Event metadata
│       ├── ErrorLoadingState.tsx # FR-008: Error/loading
│       ├── AuthCardFields.tsx    # FR-009: Card + email field
│       └── AdminTable.tsx        # FR-010: Table-head + empty-state
├── hooks/
│   └── useSuccessTimer.ts       # FR-005: Success-timer hook
└── types/
    └── index.ts                  # FR-002: Remove unused types
```

**Structure Decision**: Single-project SPA with existing layout. Extracted shared components go to `src/components/shared/` (already exists as convention from assumptions). Extracted hooks go to `src/hooks/` (existing directory). No structural changes — only additions to existing directories.

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| Principle I (TDD): No new tests for extracted shared components | Refactoring-only feature — all code extracted is existing, already-covered behavior. 233 existing tests serve as regression guard. Extracted components introduce new module boundaries but zero new logic. | Writing tests for every extracted component would double implementation time for no behavioral coverage gain (the code paths are already exercised). If future features add logic to these components, tests will be written then. |

**Rationale**: This waiver applies only to behavioral-preserving extractions. Any extracted component that receives new feature logic in the future must have tests per Principle I.
